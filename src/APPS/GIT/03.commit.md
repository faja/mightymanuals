```bash
###############################################################################
# once you added one more change (with git add), and want to add that change
# to the last commit without changing a commit message
git commit --amend --no-edit

###############################################################################
# change last commit message
git commit --amend
```
