- compose.yaml

```yaml
---
name: tmp

services:
  nginx:
    image: nginx:alpine
    restart: unless-stopped
    ports:
      - 3322:80
    volumes:
      - ./index.html:/usr/share/nginx/html/index.html:ro
```

- index.html

```yaml
lfg
```
